# Gerçek URL Yönlendirmesi — Uygulama Planı

> **Ajan çalışanlar için:** Bu planı görev görev uygulamak için `superpowers:subagent-driven-development` (önerilen) veya `superpowers:executing-plans` alt-skill'ini kullanın. Adımlar takip için checkbox (`- [ ]`) sözdizimi kullanır.

**Hedef:** 16 ekranın tek bir client-side route'ta `viewState` ile değiştiği yapıyı, dosya bazlı Next.js route'larına kademeli olarak taşımak; tarayıcı ileri/geri tuşlarını, sayfa yenilemeyi ve paylaşılabilir adresleri çalışır hale getirmek.

**Mimari:** Yeni dosyalar yalnızca *route katmanı*. Mevcut ekran bileşenleri bugünkü prop arayüzleriyle korunur; route sayfası `router.push` köprülerini geçer. İki route grubu: `(shell)` navbar'lı, `(full)` navbar'sız. Eski `src/app/page.js` son göreve kadar yerinde kalır ve çalışmaya devam eder — bu sayede her görev sonunda site tam çalışır.

**Teknoloji:** Next.js 16.2.10 (App Router, Turbopack), React 19.2.4, Tailwind v4, Supabase JS v2.

## Global Kısıtlar

- **Veritabanına dokunulmaz.** Tablo, kolon, RLS politikası, storage yapılandırması değişmez. Bu planda tek satır SQL yoktur.
- **Mevcut bileşenlerin prop arayüzleri değişmez.** Tek istisna Görev 7 (`CreateListingWizard`), o da opsiyonel.
- **Next.js 16 kuralı:** `params` bir Promise'tir; senkron erişim kaldırılmıştır. Client component'lerde `useParams()` kullanılır, `params.plate` şeklinde doğrudan erişim **yapılmaz**.
- **Import yolu:** Yeni route dosyalarında `@/` alias'ı kullanılır (`jsconfig.json`'da `"@/*": ["./src/*"]` tanımlı). Mevcut dosyalardaki relative import'lar değiştirilmez.
- **Tasarım dili:** Zemin `#FFFDFB`, metin `#0F172A`, vurgu `indigo-600`, kenarlık `slate-200`, köşe `rounded-xl`, başlık `font-black`/`font-bold`, kod/plaka `font-mono`. Yükleme göstergesi mevcut desen: `animate-spin rounded-full h-6 w-6 border-2 border-indigo-600 border-t-transparent`.
- **Metin dili:** Tüm kullanıcıya görünen metinler Türkçe.
- **Her görev sonunda commit atılır** ve site çalışır durumda bırakılır.

## Test altyapısı hakkında not

Bu projede **test çatısı yok**: `package.json`'da test script'i, Jest/Vitest/Playwright bağımlılığı bulunmuyor. Spec kapsamı yönlendirmeyle sınırlı olduğu için bu plan test çatısı kurmaz. Doğrulama her görevde **tarayıcıda elle kontrol** ile yapılır; adımlar tam olarak ne tıklanacağını ve ne görülmesi gerektiğini yazar.

Test çatısı kurmak isteniyorsa bu ayrı bir iş kalemidir ve ayrı onay gerektirir.

## Dosya yapısı

| Dosya | Sorumluluk | Görev |
|---|---|---|
| `src/components/layout/Navbar.jsx` | Üst şerit: logo, menüler, hesap açılırı, çıkış. Kendi auth state'ini yönetir. | 1 |
| `src/app/(shell)/layout.js` | Navbar'lı sayfaların sarmalayıcısı | 1 |
| `src/app/(full)/details/[plate]/page.js` | Plakayla aracı çeker, sahiplik rolünü belirler | 1 |
| `src/app/(full)/karne/[plate]/page.js` | Plakayla aracı çeker, karne ekranını açar | 1 |
| `src/app/(shell)/verify/page.js` | PIN sorgulama formu | 2 |
| `src/app/(shell)/verify/[pin]/page.js` | PIN'i otomatik sorgular, bulursa detaya yönlendirir | 2 |
| `src/app/(shell)/garage/page.js` | Garaj + bakım modalı | 3 |
| `src/app/(shell)/my-listings/page.js` | Aktif ilanlar | 3 |
| `src/app/(full)/add-vehicle/[step]/page.js` | 4 adımlı kayıt sihirbazı | 3 |
| `src/app/(full)/login/page.js` | Giriş | 4 |
| `src/app/(full)/register/page.js` | Kayıt | 4 |
| `src/app/(full)/reset-password/page.js` | Şifre sıfırlama (eski `route.js`'in yerine) | 4 |
| `src/components/common/ComingSoon.jsx` | Yer tutucu sayfa gövdesi | 5 |
| `src/app/(shell)/{dashboard,query-history,packages,account,insurance-offer,maintenance-planner}/page.js` | 6 yer tutucu | 5 |
| `src/app/(shell)/page.js` | Anasayfa (pazaryeri vitrini) | 6 |
| `src/app/page.js` | **Görev 6'da silinir** | 6 |

## Geçiş dönemi hakkında bilinmesi gerekenler

Görev 1–5 boyunca eski `src/app/page.js` yerinde kalır. Bu, geçici olarak **iki navigasyon yolu** demektir: anasayfadaki eski navbar `viewState` ile ekran değiştirir, yeni route'lar ise URL ile çalışır. İkisi de doğru çalışır, sadece anasayfada eski davranış sürer. Görev 6 eski yolu kaldırır ve tekilleştirir.

Bu bilinçli bir tercihtir: her görev sonunda çalışan bir site bırakmanın bedeli, kısa süreli bu ikilik.

---

### Görev 1: Navbar, shell layout ve araç/karne sayfaları

**Dosyalar:**
- Oluştur: `src/components/layout/Navbar.jsx`
- Oluştur: `src/app/(shell)/layout.js`
- Oluştur: `src/app/(full)/details/[plate]/page.js`
- Oluştur: `src/app/(full)/karne/[plate]/page.js`
- Değiştirilmez: `src/app/page.js`, `src/app/layout.js`, tüm ekran bileşenleri

**Arayüzler:**
- Kullanır: `VehicleDetailsScreen({ vehicle, onBack, onViewKarne, isPublicView })`, `OtoKarneScreen({ vehicle, onBack })`, `NotificationDropdown({ onNavigateToGarage })`, `supabase`
- Üretir: `/details/<plaka>` ve `/karne/<plaka>` route'ları; `Navbar` bileşeni (Görev 6'da anasayfada da kullanılacak)

**Neden bu ikisi ilk:** `/verify` sorgudan sonra `/details`'e yönlendirir. Details önce var olmalı, yoksa Görev 2 sonunda 404 alırız.

- [ ] **Adım 1: Navbar bileşenini oluştur**

`src/components/layout/Navbar.jsx`. İçerik `src/app/page.js`'in 217–379. satırlarındaki navbar JSX'inin aynısıdır; tek fark `setViewState(...)` çağrılarının `router.push(...)` ile değişmesi ve bileşenin kendi auth state'ini yönetmesi.

```jsx
// =========================================================================
// OTO-CV ÜST ŞERİT: PAYLAŞILAN NAVBAR (Navbar.jsx)
// İşlev: Logo, ana menü, İlan Ver açılır paneli, bildirim zili ve hesap
//        menüsü. Kendi oturum durumunu yönetir, sayfalardan bağımsızdır.
// =========================================================================

'use client';

import React, { useState, useEffect } from 'react';
import { useRouter, usePathname } from 'next/navigation';
import { supabase } from '@/lib/supabase';
import NotificationDropdown from '@/context/NotificationDropdown';

export default function Navbar() {
  const router = useRouter();
  const pathname = usePathname();

  const [user, setUser] = useState(null);
  const [navbarName, setNavbarName] = useState('');
  const [isDropdownOpen, setIsDropdownOpen] = useState(false);
  const [isIlanMenuOpen, setIsIlanMenuOpen] = useState(false);

  // Oturum takibi
  useEffect(() => {
    supabase.auth.getUser().then(({ data: { user: currentUser } }) => {
      setUser(currentUser);
    });

    const { data: { subscription } } = supabase.auth.onAuthStateChange((event, session) => {
      setUser(session?.user || null);
    });

    return () => subscription?.unsubscribe();
  }, []);

  // Profil adı
  useEffect(() => {
    if (!user) {
      setNavbarName('');
      return;
    }
    supabase
      .from('profiles')
      .select('first_name, last_name')
      .eq('id', user.id)
      .single()
      .then(({ data, error }) => {
        if (data && !error && data.first_name && data.last_name) {
          const formatted = `${data.first_name.charAt(0).toUpperCase()}${data.first_name.slice(1)} ${data.last_name.charAt(0).toUpperCase()}.`;
          setNavbarName(formatted);
        } else {
          setNavbarName('Hesabım');
        }
      });
  }, [user]);

  const closeMenus = () => {
    setIsDropdownOpen(false);
    setIsIlanMenuOpen(false);
  };

  const go = (path) => {
    closeMenus();
    router.push(path);
  };

  // Oturum gerektiren hedefler: yoksa girişe gönder
  const goAuthed = (path) => {
    closeMenus();
    router.push(user ? path : '/login');
  };

  const handleSignOut = async () => {
    closeMenus();
    await supabase.auth.signOut();
    setUser(null);
    setNavbarName('');
    router.push('/');
  };

  return (
    <nav className="bg-white border-b border-gray-200 sticky top-0 z-50 select-none">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-16 flex justify-between items-center">

        <div className="flex items-center gap-8">
          <span onClick={() => go('/')} className="text-base font-black tracking-tight cursor-pointer text-slate-900">
            OTO.CV
          </span>
          <div className="hidden md:flex items-center gap-6 text-xs font-bold text-slate-500">
            <span
              onClick={() => go('/')}
              className={`cursor-pointer transition-colors ${pathname === '/' ? 'text-indigo-600 font-extrabold' : 'hover:text-slate-900'}`}
            >
              Pazaryeri Vitrini
            </span>
            <span className="hover:text-slate-900 cursor-not-allowed text-slate-400">Kurumsal Çözümler</span>
          </div>
        </div>

        <div className="flex items-center gap-3">

          <div
            className="relative"
            onMouseEnter={() => setIsIlanMenuOpen(true)}
            onMouseLeave={() => setIsIlanMenuOpen(false)}
          >
            <button
              type="button"
              className="bg-amber-400 hover:bg-amber-500 text-slate-950 font-bold text-xs px-3.5 py-2 rounded-md transition-all shadow-2xs flex items-center gap-1.5 cursor-pointer border border-amber-500/30"
            >
              <span>Ücretsiz İlan Ver</span>
              <svg className={`w-3 h-3 transition-transform duration-150 ${isIlanMenuOpen ? 'rotate-180' : ''}`} fill="none" stroke="currentColor" strokeWidth="2.5" viewBox="0 0 24 24">
                <path strokeLinecap="round" strokeLinejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5" />
              </svg>
            </button>

            {isIlanMenuOpen && (
              <div className="absolute right-0 top-full pt-1.5 w-[540px] z-50 animate-fadeIn">
                <div className="bg-white border border-slate-200 rounded-xl shadow-2xl p-4 grid grid-cols-2 gap-4 border-t-2 border-t-amber-500">

                  <div
                    onClick={() => goAuthed('/add-vehicle/step1')}
                    className="bg-[#FFFDF0] hover:bg-[#FFF9D6] border border-amber-200/80 rounded-lg p-4 transition-all duration-200 cursor-pointer group flex flex-col justify-between"
                  >
                    <div>
                      <div className="w-10 h-10 rounded-full bg-amber-400 text-slate-950 font-black text-base flex items-center justify-center mx-auto mb-2 shadow-sm group-hover:scale-110 transition-transform">
                        📋
                      </div>
                      <h4 className="text-sm font-black text-slate-900 text-center mb-3 tracking-tight">
                        Sıfırdan İlan Ver
                      </h4>
                      <div className="relative pl-5 space-y-2.5 my-3 before:absolute before:left-2 before:top-2 before:bottom-2 before:w-[2px] before:bg-amber-300/80">
                        <div className="relative text-[11px] text-slate-700 font-bold flex items-center">
                          <span className="absolute -left-5 w-2.5 h-2.5 rounded-full bg-amber-400 ring-2 ring-amber-100" />
                          <span>Kataloğumuzdan aracı seç</span>
                        </div>
                        <div className="relative text-[11px] text-slate-700 font-bold flex items-center">
                          <span className="absolute -left-5 w-2.5 h-2.5 rounded-full bg-amber-400 ring-2 ring-amber-100" />
                          <span>2 dakikada ilana çıkar</span>
                        </div>
                        <div className="relative text-[11px] text-slate-700 font-bold flex items-center">
                          <span className="absolute -left-5 w-2.5 h-2.5 rounded-full bg-amber-400 ring-2 ring-amber-100" />
                          <span>Binlerce alıcıya hızlıca ulaş</span>
                        </div>
                      </div>
                    </div>
                    <button className="mt-3 w-full bg-amber-400 group-hover:bg-amber-500 text-slate-950 font-black text-xs py-2.5 rounded-md transition-colors shadow-sm cursor-pointer">
                      Sıfırdan İlan Aç &gt;
                    </button>
                  </div>

                  <div
                    onClick={() => goAuthed('/garage')}
                    className="bg-[#F0F5FF] hover:bg-[#E2ECFF] border border-indigo-200/80 rounded-lg p-4 transition-all duration-200 cursor-pointer group flex flex-col justify-between"
                  >
                    <div>
                      <div className="w-10 h-10 rounded-full bg-indigo-600 text-white font-black text-base flex items-center justify-center mx-auto mb-2 shadow-sm group-hover:scale-110 transition-transform">
                        🚗
                      </div>
                      <h4 className="text-sm font-black text-slate-900 text-center mb-3 tracking-tight">
                        Garajımdan İlan Ver
                      </h4>
                      <div className="relative pl-5 space-y-2.5 my-3 before:absolute before:left-2 before:top-2 before:bottom-2 before:w-[2px] before:bg-indigo-300">
                        <div className="relative text-[11px] text-slate-700 font-bold flex items-center">
                          <span className="absolute -left-5 w-2.5 h-2.5 rounded-full bg-indigo-600 ring-2 ring-indigo-100" />
                          <span>Tescilli aracını garajdan seç</span>
                        </div>
                        <div className="relative text-[11px] text-slate-700 font-bold flex items-center">
                          <span className="absolute -left-5 w-2.5 h-2.5 rounded-full bg-indigo-600 ring-2 ring-indigo-100" />
                          <span>Bilgiler otomatik yüklensin</span>
                        </div>
                        <div className="relative text-[11px] text-slate-700 font-bold flex items-center">
                          <span className="absolute -left-5 w-2.5 h-2.5 rounded-full bg-indigo-600 ring-2 ring-indigo-100" />
                          <span>Tek tıkla anında yayına al</span>
                        </div>
                      </div>
                    </div>
                    <button className="mt-3 w-full bg-indigo-600 group-hover:bg-indigo-700 text-white font-black text-xs py-2.5 rounded-md transition-colors shadow-sm cursor-pointer">
                      Garajdan İlan Çıkar &gt;
                    </button>
                  </div>

                </div>
              </div>
            )}
          </div>

          <NotificationDropdown onNavigateToGarage={() => goAuthed('/garage')} />

          {!user ? (
            <div className="flex items-center gap-2.5 text-xs font-bold text-slate-600 pl-1">
              <span onClick={() => go('/login')} className="hover:text-indigo-600 cursor-pointer transition-colors">Giriş Yap</span>
              <span className="text-slate-200">|</span>
              <span onClick={() => go('/register')} className="hover:text-indigo-600 cursor-pointer transition-colors">Hesap Aç</span>
            </div>
          ) : (
            <div className="relative pl-1">
              <button
                type="button"
                onClick={() => setIsDropdownOpen(!isDropdownOpen)}
                className="flex items-center gap-1.5 text-xs font-bold text-slate-800 hover:text-indigo-600 transition-colors cursor-pointer"
              >
                <span>{navbarName || 'Hesabım'}</span>
                <svg className={`w-3.5 h-3.5 transition-transform duration-200 ${isDropdownOpen ? 'rotate-180' : ''}`} fill="none" stroke="currentColor" strokeWidth="2.5" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5" /></svg>
              </button>

              {isDropdownOpen && (
                <div className="absolute right-0 mt-3 w-64 bg-white border border-slate-200 rounded-lg shadow-xl overflow-hidden z-50 text-sm">
                  <div className="bg-slate-50/80 px-4 py-3 border-b border-slate-100 flex flex-col">
                    <span className="text-[10px] font-bold text-slate-400 tracking-wider uppercase">Hesap Detayı</span>
                    <span className="text-slate-800 font-semibold text-xs truncate mt-0.5">{user.email}</span>
                  </div>

                  <div className="py-1 flex flex-col">
                    <DropdownRow title="Bana Özel Özet" onClick={() => go('/dashboard')} />
                    <DropdownRow title="Tescilli Taşıtlarım (Garaj)" onClick={() => go('/garage')} />
                    <DropdownRow title="Aktif İlanlarım" onClick={() => go('/my-listings')} />
                    <DropdownRow title="Sorgulama Geçmişim" onClick={() => go('/query-history')} />
                    <DropdownRow title="Paketlerim & Ödemeler" onClick={() => go('/packages')} />
                    <DropdownRow title="Hesabım" onClick={() => go('/account')} />
                  </div>

                  <div className="border-t border-slate-100 p-1.5">
                    <button
                      type="button"
                      onClick={handleSignOut}
                      className="w-full px-3 py-2 text-left font-semibold text-rose-600 hover:bg-rose-50 rounded flex items-center justify-between transition-colors cursor-pointer text-xs group"
                    >
                      <span>Çıkış Yap</span>
                      <svg className="w-4 h-4 text-rose-400 group-hover:text-rose-600 transition-colors" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24">
                        <path strokeLinecap="round" strokeLinejoin="round" d="M15.75 9V5.25A2.25 2.25 0 0013.5 3h-6a2.25 2.25 0 00-2.25 2.25V15M12 9l3 3m0 0l-3 3m3-3H8.25" />
                      </svg>
                    </button>
                  </div>
                </div>
              )}
            </div>
          )}
        </div>
      </div>
    </nav>
  );
}

function DropdownRow({ title, onClick }) {
  return (
    <div
      onClick={onClick}
      className="px-4 py-2.5 hover:bg-slate-50 cursor-pointer flex justify-between items-center transition-colors text-slate-700 font-semibold text-xs group"
    >
      <span>{title}</span>
      <svg className="w-3.5 h-3.5 text-slate-300 group-hover:text-slate-400 transition-colors" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24">
        <path strokeLinecap="round" strokeLinejoin="round" d="M9 5l7 7-7 7" />
      </svg>
    </div>
  );
}
```

> Not: Orijinal kodda `font-display` sınıfı logoda kullanılıyordu. Tailwind v4'te `--font-display` `@theme` bloğunda tanımlı olmadığı için bu sınıf üretilmiyor ve hiçbir etkisi yok; bu yüzden aktarılmadı. Görünüm değişmez.

- [ ] **Adım 2: Shell layout'u oluştur**

`src/app/(shell)/layout.js`:

```jsx
// =========================================================================
// OTO-CV NAVBAR'LI SAYFA GRUBU SARMALAYICISI ((shell)/layout.js)
// İşlev: Üst şeridi bir kez basar, altındaki tüm sayfalara ortak gövde verir.
// =========================================================================

'use client';

import React from 'react';
import Navbar from '@/components/layout/Navbar';

export default function ShellLayout({ children }) {
  return (
    <div className="min-h-screen bg-[#FFFDFB] text-[#0F172A] font-sans antialiased tracking-tight">
      <Navbar />
      {children}
    </div>
  );
}
```

- [ ] **Adım 3: Araç detay sayfasını oluştur**

`src/app/(full)/details/[plate]/page.js`. Bu sayfa aracı plakayla çeker ve sahiplik karşılaştırmasıyla rolü belirler (spec 7.3).

```jsx
// =========================================================================
// OTO-CV ARAÇ DETAY ROUTE'U ((full)/details/[plate]/page.js)
// İşlev: URL'deki plakayla aracı buluttan çeker, oturum sahibiyle
//        karşılaştırıp sahip/ziyaretçi rolünü belirler.
// =========================================================================

'use client';

import React, { useState, useEffect } from 'react';
import { useParams, useRouter } from 'next/navigation';
import { supabase } from '@/lib/supabase';
import VehicleDetailsScreen from '@/components/VehicleDetailsScreen';

export default function VehicleDetailsPage() {
  const router = useRouter();
  const params = useParams();
  const plate = decodeURIComponent(params.plate);

  const [vehicle, setVehicle] = useState(null);
  const [isOwner, setIsOwner] = useState(false);
  const [status, setStatus] = useState('loading'); // 'loading' | 'ready' | 'notfound'

  useEffect(() => {
    let cancelled = false;

    const loadVehicle = async () => {
      setStatus('loading');

      const { data, error } = await supabase
        .from('vehicles')
        .select('*')
        .eq('plate_number', plate)
        .maybeSingle();

      if (cancelled) return;

      if (error || !data) {
        setStatus('notfound');
        return;
      }

      const { data: { user } } = await supabase.auth.getUser();
      if (cancelled) return;

      setVehicle(data);
      setIsOwner(!!user && user.id === data.user_id);
      setStatus('ready');
    };

    loadVehicle();
    return () => { cancelled = true; };
  }, [plate]);

  if (status === 'loading') {
    return (
      <div className="min-h-screen bg-[#FFFDFB] flex flex-col items-center justify-center gap-3">
        <div className="animate-spin rounded-full h-6 w-6 border-2 border-indigo-600 border-t-transparent" />
        <span className="text-[11px] font-bold text-slate-400 tracking-wide">Araç sicili yükleniyor...</span>
      </div>
    );
  }

  if (status === 'notfound') {
    return (
      <div className="min-h-screen bg-[#FFFDFB] flex items-center justify-center p-4">
        <div className="bg-white border border-gray-200 rounded-2xl p-8 max-w-md w-full text-center space-y-4 shadow-sm">
          <h1 className="text-lg font-black text-slate-900 tracking-tight">Araç bulunamadı</h1>
          <p className="text-xs text-slate-500 font-medium leading-relaxed">
            <span className="font-mono font-bold text-slate-700">{plate}</span> plakasına ait tescilli bir kayıt
            bulunamadı. Plakayı kontrol edin ya da PIN kodu ile sorgulayın.
          </p>
          <div className="flex gap-2 pt-1">
            <button
              type="button"
              onClick={() => router.push('/verify')}
              className="flex-1 bg-indigo-600 hover:bg-indigo-700 text-white font-bold text-xs py-3 rounded-xl transition-colors cursor-pointer"
            >
              PIN ile Sorgula
            </button>
            <button
              type="button"
              onClick={() => router.push('/')}
              className="flex-1 bg-white hover:bg-slate-50 text-slate-800 border border-gray-200 font-bold text-xs py-3 rounded-xl transition-colors cursor-pointer"
            >
              Anasayfa
            </button>
          </div>
        </div>
      </div>
    );
  }

  return (
    <VehicleDetailsScreen
      vehicle={vehicle}
      isPublicView={!isOwner}
      onBack={() => router.back()}
      onViewKarne={() => router.push(`/karne/${encodeURIComponent(plate)}`)}
    />
  );
}
```

- [ ] **Adım 4: Karne sayfasını oluştur**

`src/app/(full)/karne/[plate]/page.js`:

```jsx
// =========================================================================
// OTO-CV KARNE ROUTE'U ((full)/karne/[plate]/page.js)
// İşlev: URL'deki plakayla aracı çeker ve Oto-Karne ekranını açar.
// =========================================================================

'use client';

import React, { useState, useEffect } from 'react';
import { useParams, useRouter } from 'next/navigation';
import { supabase } from '@/lib/supabase';
import OtoKarneScreen from '@/components/karne/OtoKarneScreen';

export default function KarnePage() {
  const router = useRouter();
  const params = useParams();
  const plate = decodeURIComponent(params.plate);

  const [vehicle, setVehicle] = useState(null);
  const [status, setStatus] = useState('loading'); // 'loading' | 'ready' | 'notfound'

  useEffect(() => {
    let cancelled = false;

    const loadVehicle = async () => {
      setStatus('loading');

      const { data, error } = await supabase
        .from('vehicles')
        .select('*')
        .eq('plate_number', plate)
        .maybeSingle();

      if (cancelled) return;

      if (error || !data) {
        setStatus('notfound');
        return;
      }

      setVehicle(data);
      setStatus('ready');
    };

    loadVehicle();
    return () => { cancelled = true; };
  }, [plate]);

  if (status === 'loading') {
    return (
      <div className="min-h-screen bg-[#F8FAFC] flex flex-col items-center justify-center gap-3">
        <div className="animate-spin rounded-full h-6 w-6 border-2 border-indigo-600 border-t-transparent" />
        <span className="text-[11px] font-bold text-slate-400 tracking-wide">Resmi sicil derleniyor...</span>
      </div>
    );
  }

  if (status === 'notfound') {
    return (
      <div className="min-h-screen bg-[#F8FAFC] flex items-center justify-center p-4">
        <div className="bg-white border border-gray-200 rounded-2xl p-8 max-w-md w-full text-center space-y-4 shadow-sm">
          <h1 className="text-lg font-black text-slate-900 tracking-tight">Karne bulunamadı</h1>
          <p className="text-xs text-slate-500 font-medium leading-relaxed">
            <span className="font-mono font-bold text-slate-700">{plate}</span> plakasına ait tescilli bir kayıt yok.
          </p>
          <button
            type="button"
            onClick={() => router.push('/')}
            className="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold text-xs py-3 rounded-xl transition-colors cursor-pointer"
          >
            Anasayfaya Dön
          </button>
        </div>
      </div>
    );
  }

  return <OtoKarneScreen vehicle={vehicle} onBack={() => router.back()} />;
}
```

- [ ] **Adım 5: Geliştirme sunucusunu başlat ve doğrula**

Çalıştır: `npm run dev`

Tarayıcıda sırayla kontrol et. `<PLAKA>` yerine garajındaki gerçek bir plakayı yaz (boşluklar otomatik kodlanır, adres çubuğuna boşluklu da yazabilirsin):

1. `http://localhost:3000/` → anasayfa eskisi gibi açılıyor, hiçbir şey değişmemiş
2. `http://localhost:3000/details/<PLAKA>` → araç detayı açılıyor, görünüm bozulmamış
3. Aynı sayfada F5 → **aynı sayfada kalıyor** (eski yapıda anasayfaya düşerdi)
4. Detay sayfasından karne butonuna bas → `/karne/<PLAKA>` adresine gidiyor
5. Tarayıcının **geri** tuşu → detay sayfasına dönüyor
6. Tarayıcının **ileri** tuşu → karneye geri geliyor
7. `http://localhost:3000/details/YOKBOYLE` → "Araç bulunamadı" kartı çıkıyor, çökme yok
8. Tarayıcı konsolunda yeni hata yok

Beklenen: 8 maddenin tamamı geçer.

- [ ] **Adım 6: Commit**

```bash
git add src/components/layout/Navbar.jsx "src/app/(shell)/layout.js" "src/app/(full)/details/[plate]/page.js" "src/app/(full)/karne/[plate]/page.js"
git commit -m "feat(routing): araç detay ve karne route'ları + paylaşılan navbar"
```

---

### Görev 2: PIN doğrulama route'ları — kazanım hunisinin onarımı

**Dosyalar:**
- Oluştur: `src/app/(shell)/verify/page.js`
- Oluştur: `src/app/(shell)/verify/[pin]/page.js`
- Değiştir: `src/components/karne/OtoKarneScreen.jsx:120-126` (paylaşım linki) ve `:246` (gösterilen link metni)
- Değiştir: `src/components/VehicleVerificationScreen.jsx` (`mockVehicles` çökmesinin giderilmesi)

**Arayüzler:**
- Kullanır: `VehicleVerificationScreen({ onVehicleFound })`, Görev 1'de üretilen `/details/<plaka>`
- Üretir: `/verify` ve `/verify/<pin>` route'ları

**Bu görevin değeri:** Ürünün tek kazanım kanalı olan karne linki şu anda ölü. Bu görev onu çalıştırır.

- [ ] **Adım 1: `VehicleVerificationScreen`'deki çökmeyi gider**

Bileşen `page.js`'in hiç göndermediği `mockVehicles` prop'unu kullanıyor; araç bulunamayınca `undefined.find` hatası atıyor ve `catch` bloğu aynı hatayı tekrarlıyor. `mockVehicles`'a yapılan tüm başvurular kaldırılır, yerine düzgün hata durumu gelir.

`src/components/VehicleVerificationScreen.jsx` içinde bileşen imzasını ve iki fonksiyonu değiştir:

```jsx
export default function VehicleVerificationScreen({ onVehicleFound, initialPin = '' }) {
  const [verifyTab, setVerifyTab] = useState('pin');
  const [searchPin, setSearchPin] = useState(initialPin);
  const [isQrScanning, setIsQrScanning] = useState(false);
  const [loading, setLoading] = useState(false);
  const [searchError, setSearchError] = useState('');
```

`handlePinSearch` fonksiyonunun tamamını şununla değiştir:

```jsx
  const handlePinSearch = async (e) => {
    if (e) e.preventDefault();
    const cleanPin = searchPin.trim();
    if (!cleanPin) return;

    setSearchError('');
    setLoading(true);

    try {
      const { data, error } = await supabase
        .from('vehicles')
        .select('*')
        .ilike('pin_code', cleanPin);

      if (error) throw error;

      if (data && data.length > 0) {
        onVehicleFound(data[0], 'public');
        return;
      }

      setSearchError('Bu PIN koduna ait aktif bir araç kaydı bulunamadı. Kodu kontrol edip tekrar deneyin.');
    } catch (err) {
      console.error('Sorgulama hatası:', err);
      setSearchError('Sorgulama sırasında bir hata oluştu. Lütfen tekrar deneyin.');
    } finally {
      setLoading(false);
    }
  };
```

`handleQuickChipQuery` fonksiyonunun tamamını şununla değiştir:

```jsx
  const handleQuickChipQuery = async (code) => {
    setSearchPin(code);
    setSearchError('');
    setLoading(true);

    try {
      const { data, error } = await supabase
        .from('vehicles')
        .select('*')
        .ilike('pin_code', code);

      if (error) throw error;

      if (data && data.length > 0) {
        onVehicleFound(data[0], 'public');
        return;
      }

      setSearchError(`${code} koduna ait bir araç kaydı bulunamadı.`);
    } catch (err) {
      console.error('Sorgulama hatası:', err);
      setSearchError('Sorgulama sırasında bir hata oluştu. Lütfen tekrar deneyin.');
    } finally {
      setLoading(false);
    }
  };
```

`triggerQrScannerSimulation` içindeki `mockVehicles` yedeğini kaldır — `setTimeout` gövdesini şununla değiştir:

```jsx
      setTimeout(() => {
        setIsQrScanning(false);
        if (data && data.length > 0) {
          onVehicleFound(data[0], 'public');
        } else {
          setSearchError('Taranacak tescilli araç bulunamadı.');
        }
      }, 2000);
```

Hata mesajını göstermek için PIN input'unun (satır ~163-172, `<div className="relative">` bloğu) hemen altına ekle:

```jsx
              {searchError && (
                <div className="bg-rose-50 border border-rose-100 rounded-xl px-4 py-3 flex gap-2.5 items-start">
                  <span className="w-1.5 h-1.5 bg-rose-500 rounded-full shrink-0 mt-1.5" />
                  <p className="text-[11px] font-semibold text-rose-700 leading-relaxed">{searchError}</p>
                </div>
              )}
```

- [ ] **Adım 2: PIN formu sayfasını oluştur**

`src/app/(shell)/verify/page.js`:

```jsx
// =========================================================================
// OTO-CV PIN SORGULAMA ROUTE'U ((shell)/verify/page.js)
// İşlev: PIN sorgulama formunu açar, araç bulunduğunda detay adresine taşır.
// =========================================================================

'use client';

import React from 'react';
import { useRouter } from 'next/navigation';
import VehicleVerificationScreen from '@/components/VehicleVerificationScreen';

export default function VerifyPage() {
  const router = useRouter();

  return (
    <div className="animate-fadeIn">
      <VehicleVerificationScreen
        onVehicleFound={(car) => router.push(`/details/${encodeURIComponent(car.plate_number)}`)}
      />
    </div>
  );
}
```

- [ ] **Adım 3: PIN'li doğrudan sorgu sayfasını oluştur**

`src/app/(shell)/verify/[pin]/page.js`. Karne linki bu adrese gelir; PIN otomatik sorgulanır ve bulunursa detaya `replace` ile taşınır (geri tuşunda sorgu ekranına düşmemek için).

```jsx
// =========================================================================
// OTO-CV PIN'Lİ DOĞRUDAN SORGU ROUTE'U ((shell)/verify/[pin]/page.js)
// İşlev: Karne üzerindeki PIN ile gelen ziyaretçiyi otomatik sorgulayıp
//        aracın detay sayfasına taşır. Bulunamazsa forma hata ile düşer.
// =========================================================================

'use client';

import React, { useState, useEffect } from 'react';
import { useParams, useRouter } from 'next/navigation';
import { supabase } from '@/lib/supabase';
import VehicleVerificationScreen from '@/components/VehicleVerificationScreen';

export default function VerifyWithPinPage() {
  const router = useRouter();
  const params = useParams();
  const pin = decodeURIComponent(params.pin);

  const [status, setStatus] = useState('searching'); // 'searching' | 'notfound'

  useEffect(() => {
    let cancelled = false;

    const lookup = async () => {
      const { data, error } = await supabase
        .from('vehicles')
        .select('plate_number')
        .ilike('pin_code', pin);

      if (cancelled) return;

      if (!error && data && data.length > 0) {
        router.replace(`/details/${encodeURIComponent(data[0].plate_number)}`);
        return;
      }

      setStatus('notfound');
    };

    lookup();
    return () => { cancelled = true; };
  }, [pin, router]);

  if (status === 'searching') {
    return (
      <div className="min-h-[60vh] flex flex-col items-center justify-center gap-3">
        <div className="animate-spin rounded-full h-6 w-6 border-2 border-indigo-600 border-t-transparent" />
        <span className="text-[11px] font-bold text-slate-400 tracking-wide">
          <span className="font-mono">{pin}</span> kodu sorgulanıyor...
        </span>
      </div>
    );
  }

  // Bulunamadıysa formu PIN dolu olarak göster; kullanıcı düzeltip tekrar denesin
  return (
    <div className="animate-fadeIn">
      <VehicleVerificationScreen
        initialPin={pin}
        onVehicleFound={(car) => router.push(`/details/${encodeURIComponent(car.plate_number)}`)}
      />
    </div>
  );
}
```

- [ ] **Adım 4: Karne paylaşım linkini düzelt**

`src/components/karne/OtoKarneScreen.jsx` içinde `copyVerificationLink` fonksiyonunu değiştir:

```jsx
  const copyVerificationLink = () => {
    const secureLink = `${window.location.origin}/verify/${encodeURIComponent(pinCode)}`;
    navigator.clipboard.writeText(secureLink);
    setShowShareModal(false);
    showToast('Sorgulama bağlantısı panoya kopyalandı.', 'success');
  };
```

Ve modalda kullanıcıya gösterilen yanlış metni (satır ~246) gerçek linkle eşitle:

```jsx
                <span className="text-[10px] text-indigo-600 font-mono truncate block mt-0.5">
                  {typeof window !== 'undefined' ? `${window.location.origin}/verify/${pinCode}` : `/verify/${pinCode}`}
                </span>
```

- [ ] **Adım 5: Doğrula**

Çalıştır: `npm run dev`

1. `http://localhost:3000/verify` → PIN formu navbar ile açılıyor
2. Garajındaki bir aracın gerçek PIN kodunu gir, "Araç Sicilini Sorgula" → `/details/<PLAKA>` açılıyor
3. Geri tuşu → `/verify` formuna dönüyor
4. Uydurma bir PIN gir (`CV-YOK99`) → **kırmızı hata kartı çıkıyor, spinner donmuyor, sayfa çökmüyor**
5. "HIZLI TEST ÖRNEKLERİ" çiplerinden birine bas → araç varsa açılıyor, yoksa hata mesajı (eskiden spinner sonsuza dönerdi)
6. `http://localhost:3000/verify/<GERÇEK-PIN>` → doğrudan araç detayına yönleniyor
7. `http://localhost:3000/verify/CV-YOK99` → form PIN dolu ve hata mesajıyla açılıyor
8. **Huni testi:** `/karne/<PLAKA>` → "Paylaş" → "Linki Kopyala" → linki yeni sekmeye yapıştır → araç detayı açılıyor. Modalda gösterilen link ile kopyalanan link **aynı**.

Beklenen: 8 maddenin tamamı geçer. Madde 8 bu görevin asıl teslimatıdır.

- [ ] **Adım 6: Commit**

```bash
git add "src/app/(shell)/verify/page.js" "src/app/(shell)/verify/[pin]/page.js" src/components/karne/OtoKarneScreen.jsx src/components/VehicleVerificationScreen.jsx
git commit -m "fix(verify): PIN doğrulama route'ları ve çalışan karne paylaşım linki

- mockVehicles kaynaklı çökme giderildi, düzgün hata durumu eklendi
- karne linki artık /verify/[pin] adresine gidiyor
- modalda gösterilen link ile kopyalanan link eşitlendi"
```

---

### Görev 3: Garaj, ilanlar ve kayıt sihirbazı route'ları

**Dosyalar:**
- Oluştur: `src/app/(shell)/garage/page.js`
- Oluştur: `src/app/(shell)/my-listings/page.js`
- Oluştur: `src/app/(full)/add-vehicle/[step]/page.js`
- Değiştirilmez: `GarageScreen.jsx`, `MyListingsScreen.jsx`, `CreateListingWizard.jsx`, `MaintenanceDialog.jsx`

**Arayüzler:**
- Kullanır: `GarageScreen({ onViewDetails, onViewKarne, onOpenMaintenance, onNavigateToAdd })`, `MyListingsScreen({ user, onNavigateToGarage })`, `MaintenanceDialog({ isOpen, vehicle, onClose, onRecordAdded })`, `CreateListingWizard({ user, onBack, onSuccess })`
- Üretir: `/garage`, `/my-listings`, `/add-vehicle/<step>` route'ları

**Not:** Bu görevde sihirbazın adım state'i URL'e bağlanmaz — `/add-vehicle/step1` sihirbazı açar, adımlar bugün olduğu gibi bileşenin kendi içinde ilerler. URL ile adım eşlemesi Görev 7'de, ayrı ve opsiyonel olarak yapılır. Bu, 1111 satırlık dosyaya dokunma riskini bu görevden ayırır.

- [ ] **Adım 1: Garaj sayfasını oluştur**

`src/app/(shell)/garage/page.js`. Oturum kontrolü sayfanın içinde yapılır (spec 7.5).

```jsx
// =========================================================================
// OTO-CV GARAJ ROUTE'U ((shell)/garage/page.js)
// İşlev: Oturum kontrolü yapar, garaj ekranını açar ve bakım modalını
//        sayfa içi state olarak yönetir.
// =========================================================================

'use client';

import React, { useState, useEffect } from 'react';
import { useRouter } from 'next/navigation';
import { supabase } from '@/lib/supabase';
import GarageScreen from '@/components/GarageScreen';
import MaintenanceDialog from '@/components/garage/MaintenanceDialog';

export default function GaragePage() {
  const router = useRouter();
  const [authState, setAuthState] = useState('checking'); // 'checking' | 'ready'
  const [activeMaintenanceVehicle, setActiveMaintenanceVehicle] = useState(null);

  useEffect(() => {
    let cancelled = false;

    supabase.auth.getUser().then(({ data: { user } }) => {
      if (cancelled) return;
      if (!user) {
        router.replace('/login');
        return;
      }
      setAuthState('ready');
    });

    return () => { cancelled = true; };
  }, [router]);

  if (authState === 'checking') {
    return (
      <div className="min-h-[60vh] flex flex-col items-center justify-center gap-3">
        <div className="animate-spin rounded-full h-6 w-6 border-2 border-indigo-600 border-t-transparent" />
        <span className="text-[11px] font-bold text-slate-400 tracking-wide">Garajınız hazırlanıyor...</span>
      </div>
    );
  }

  return (
    <div className="animate-fadeIn relative">
      <GarageScreen
        onViewDetails={(car) => router.push(`/details/${encodeURIComponent(car.plate_number)}`)}
        onViewKarne={(car) => router.push(`/karne/${encodeURIComponent(car.plate_number)}`)}
        onOpenMaintenance={(incomingVehicle) => setActiveMaintenanceVehicle(incomingVehicle)}
        onNavigateToAdd={() => router.push('/add-vehicle/step1')}
      />
      {activeMaintenanceVehicle && (
        <MaintenanceDialog
          isOpen={true}
          vehicle={activeMaintenanceVehicle}
          onClose={() => setActiveMaintenanceVehicle(null)}
          onRecordAdded={() => {}}
        />
      )}
    </div>
  );
}
```

- [ ] **Adım 2: İlanlarım sayfasını oluştur**

`src/app/(shell)/my-listings/page.js`. Bu bileşen `user` prop'u bekliyor, sayfa onu sağlar.

```jsx
// =========================================================================
// OTO-CV AKTİF İLANLARIM ROUTE'U ((shell)/my-listings/page.js)
// İşlev: Oturumu doğrular, kullanıcı nesnesini ekrana geçirir.
// =========================================================================

'use client';

import React, { useState, useEffect } from 'react';
import { useRouter } from 'next/navigation';
import { supabase } from '@/lib/supabase';
import MyListingsScreen from '@/components/marketplace/MyListingsScreen';

export default function MyListingsPage() {
  const router = useRouter();
  const [user, setUser] = useState(null);
  const [authState, setAuthState] = useState('checking'); // 'checking' | 'ready'

  useEffect(() => {
    let cancelled = false;

    supabase.auth.getUser().then(({ data: { user: currentUser } }) => {
      if (cancelled) return;
      if (!currentUser) {
        router.replace('/login');
        return;
      }
      setUser(currentUser);
      setAuthState('ready');
    });

    return () => { cancelled = true; };
  }, [router]);

  if (authState === 'checking') {
    return (
      <div className="min-h-[60vh] flex flex-col items-center justify-center gap-3">
        <div className="animate-spin rounded-full h-6 w-6 border-2 border-indigo-600 border-t-transparent" />
        <span className="text-[11px] font-bold text-slate-400 tracking-wide">İlanlarınız yükleniyor...</span>
      </div>
    );
  }

  return <MyListingsScreen user={user} onNavigateToGarage={() => router.push('/garage')} />;
}
```

- [ ] **Adım 3: Kayıt sihirbazı sayfasını oluştur**

`src/app/(full)/add-vehicle/[step]/page.js`:

```jsx
// =========================================================================
// OTO-CV ARAÇ KAYIT SİHİRBAZI ROUTE'U ((full)/add-vehicle/[step]/page.js)
// İşlev: 4 adımlı kayıt sihirbazını tam sayfa açar. Adım ilerlemesi şu an
//        sihirbazın kendi içinde yönetiliyor; URL eşlemesi Görev 7'de.
// =========================================================================

'use client';

import React, { useState, useEffect } from 'react';
import { useRouter } from 'next/navigation';
import { supabase } from '@/lib/supabase';
import CreateListingWizard from '@/components/marketplace/create-listing/CreateListingWizard';

export default function AddVehiclePage() {
  const router = useRouter();
  const [user, setUser] = useState(null);
  const [authState, setAuthState] = useState('checking'); // 'checking' | 'ready'

  useEffect(() => {
    let cancelled = false;

    supabase.auth.getUser().then(({ data: { user: currentUser } }) => {
      if (cancelled) return;
      if (!currentUser) {
        router.replace('/login');
        return;
      }
      setUser(currentUser);
      setAuthState('ready');
    });

    return () => { cancelled = true; };
  }, [router]);

  if (authState === 'checking') {
    return (
      <div className="min-h-screen bg-[#FFFDFB] flex flex-col items-center justify-center gap-3">
        <div className="animate-spin rounded-full h-6 w-6 border-2 border-indigo-600 border-t-transparent" />
        <span className="text-[11px] font-bold text-slate-400 tracking-wide">Sihirbaz hazırlanıyor...</span>
      </div>
    );
  }

  return (
    <CreateListingWizard
      user={user}
      onBack={() => router.push('/garage')}
      onSuccess={() => router.push('/garage')}
    />
  );
}
```

- [ ] **Adım 4: Doğrula**

Çalıştır: `npm run dev`. Oturum açık olarak:

1. `http://localhost:3000/garage` → garaj açılıyor, araçlar listeleniyor, görünüm bozulmamış
2. F5 → garajda kalıyor
3. Bir araçta "Detay" → `/details/<PLAKA>`; geri tuşu → garaja dönüyor
4. Bir araçta "Karne" → `/karne/<PLAKA>`; geri tuşu → garaja dönüyor
5. "Bakım Ekle" → modal açılıyor (URL değişmiyor, doğrusu bu); kapat → garaj duruyor
6. Garajda "araç ekle" → `/add-vehicle/step1` açılıyor, sihirbaz 1. adımda
7. Sihirbazda "Geri" → `/garage`
8. `http://localhost:3000/my-listings` → ilanlar açılıyor
9. **Oturumu kapat**, sonra `http://localhost:3000/garage` yaz → `/login`'e yönleniyor (bu sayfa Görev 4'te gelecek; şimdilik 404 görmek **beklenen**)
10. Konsolda yeni hata yok

Beklenen: 1–8 ve 10 geçer. Madde 9'da 404 görmek normaldir, Görev 4 bunu kapatır.

- [ ] **Adım 5: Commit**

```bash
git add "src/app/(shell)/garage/page.js" "src/app/(shell)/my-listings/page.js" "src/app/(full)/add-vehicle/[step]/page.js"
git commit -m "feat(routing): garaj, ilanlarım ve araç kayıt sihirbazı route'ları"
```

---

### Görev 4: Kimlik doğrulama route'ları

**Dosyalar:**
- Oluştur: `src/app/(full)/login/page.js`
- Oluştur: `src/app/(full)/register/page.js`
- Oluştur: `src/app/(full)/reset-password/page.js`
- Sil: `src/app/reset-password/route.js`
- Değiştirilmez: `VehicleAuthScreen.jsx`, `ResetPasswordScreen.jsx`

**Arayüzler:**
- Kullanır: `VehicleAuthScreen({ initialMode, onAuthSuccess, onBack })`, `ResetPasswordScreen({ onSuccess, onBack })`
- Üretir: `/login`, `/register`, `/reset-password` route'ları

**Dikkat:** `src/app/reset-password/route.js` siliniyor. Next.js'te aynı klasörde `route.js` ve `page.js` birlikte bulunamaz. Bu, şifre sıfırlama akışını değiştiren tek adımdır ve **gerçek e-posta ile test edilmelidir.**

- [ ] **Adım 1: Giriş sayfasını oluştur**

`src/app/(full)/login/page.js`:

```jsx
// =========================================================================
// OTO-CV GİRİŞ ROUTE'U ((full)/login/page.js)
// =========================================================================

'use client';

import React from 'react';
import { useRouter } from 'next/navigation';
import VehicleAuthScreen from '@/components/VehicleAuthScreen';

export default function LoginPage() {
  const router = useRouter();

  return (
    <VehicleAuthScreen
      initialMode="login"
      onAuthSuccess={() => router.push('/garage')}
      onBack={() => router.push('/')}
    />
  );
}
```

- [ ] **Adım 2: Kayıt sayfasını oluştur**

`src/app/(full)/register/page.js`:

```jsx
// =========================================================================
// OTO-CV HESAP AÇMA ROUTE'U ((full)/register/page.js)
// =========================================================================

'use client';

import React from 'react';
import { useRouter } from 'next/navigation';
import VehicleAuthScreen from '@/components/VehicleAuthScreen';

export default function RegisterPage() {
  const router = useRouter();

  return (
    <VehicleAuthScreen
      initialMode="register_step1"
      onAuthSuccess={() => router.push('/garage')}
      onBack={() => router.push('/')}
    />
  );
}
```

- [ ] **Adım 3: Eski yönlendirme route'unu sil**

```bash
git rm "src/app/reset-password/route.js"
```

- [ ] **Adım 4: Şifre sıfırlama sayfasını oluştur**

`src/app/(full)/reset-password/page.js`. Supabase kurtarma linki bu adrese gelir; `supabase.js` içinde `detectSessionInUrl: true` ayarı zaten açık olduğu için token istemci tarafında otomatik işlenir.

```jsx
// =========================================================================
// OTO-CV ŞİFRE SIFIRLAMA ROUTE'U ((full)/reset-password/page.js)
// İşlev: E-posta kurtarma linkinin indiği adres. Eski route.js yönlendirme
//        hilesinin yerini alır; token'ı Supabase istemcisi kendisi işler
//        (detectSessionInUrl: true).
// =========================================================================

'use client';

import React from 'react';
import { useRouter } from 'next/navigation';
import ResetPasswordScreen from '@/components/ResetPasswordScreen';

export default function ResetPasswordPage() {
  const router = useRouter();

  return (
    <ResetPasswordScreen
      onSuccess={() => router.push('/')}
      onBack={() => router.push('/')}
    />
  );
}
```

- [ ] **Adım 5: Doğrula**

Çalıştır: `npm run dev`

1. `http://localhost:3000/login` → giriş ekranı tam sayfa (navbar yok), görünüm bozulmamış
2. Gerçek hesapla giriş yap → `/garage` açılıyor
3. Çıkış yap, `http://localhost:3000/garage` yaz → `/login`'e yönleniyor (Görev 3, madde 9 artık geçiyor)
4. `http://localhost:3000/register` → kayıt ekranı açılıyor
5. Navbar'daki "Giriş Yap" ve "Hesap Aç" bağlantıları doğru adreslere gidiyor
6. Konsolda yeni hata yok

- [ ] **Adım 6: Şifre sıfırlamayı canlı test et**

Bu adım kullanıcı ile birlikte yapılmalıdır, tek başına doğrulanamaz.

1. `/login` ekranından "Şifremi Unuttum" akışını başlat, gerçek e-posta adresi gir
2. Gelen e-postadaki linke tıkla
3. Beklenen: `/reset-password` sayfası açılıyor ve **yeni şifre belirleme formu** görünüyor
4. Yeni şifre belirle, kaydet
5. Yeni şifreyle giriş yapılabildiğini doğrula

**Bu adım başarısız olursa:** `git revert` ile bu görev geri alınır ve spec'in 9. bölümündeki yedek plana geçilir (`route.js` yerinde bırakılıp ekran `/reset-password/form` adresine alınır).

- [ ] **Adım 7: Commit**

```bash
git add "src/app/(full)/login/page.js" "src/app/(full)/register/page.js" "src/app/(full)/reset-password/page.js"
git commit -m "feat(routing): giriş, kayıt ve şifre sıfırlama route'ları

Eski src/app/reset-password/route.js yönlendirme hilesi kaldırıldı;
kurtarma linki artık doğrudan sayfaya iniyor."
```

---

### Görev 5: Yer tutucu sayfalar

**Dosyalar:**
- Oluştur: `src/components/common/ComingSoon.jsx`
- Oluştur: `src/app/(shell)/dashboard/page.js`
- Oluştur: `src/app/(shell)/query-history/page.js`
- Oluştur: `src/app/(shell)/packages/page.js`
- Oluştur: `src/app/(shell)/account/page.js`
- Oluştur: `src/app/(shell)/insurance-offer/page.js`
- Oluştur: `src/app/(shell)/maintenance-planner/page.js`

**Arayüzler:**
- Üretir: `ComingSoon({ title, description })` bileşeni ve 6 route

**Bu görevin değeri:** Bu 6 ekran bugün `viewState`'e yazılıyor ama render tarafında karşılığı olmadığı için sessizce pazaryerine düşüyor. Kullanıcı tıkladığında hiçbir geri bildirim almıyor. Route'lar bunu bitirir.

- [ ] **Adım 1: ComingSoon bileşenini oluştur**

`src/components/common/ComingSoon.jsx`:

```jsx
// =========================================================================
// OTO-CV YER TUTUCU SAYFA GÖVDESİ (ComingSoon.jsx)
// İşlev: Henüz tamamlanmamış ekranlar için dürüst bir bilgi kartı.
// =========================================================================

'use client';

import React from 'react';
import { useRouter } from 'next/navigation';

export default function ComingSoon({ title, description }) {
  const router = useRouter();

  return (
    <div className="max-w-3xl mx-auto px-4 py-16 animate-fadeIn">
      <div className="bg-white border border-gray-200 rounded-2xl p-8 md:p-10 shadow-sm space-y-5 text-center">
        <span className="inline-flex items-center gap-1.5 px-3 py-1 bg-indigo-50 border border-indigo-100 rounded-full text-indigo-600 text-[10px] font-black tracking-wider uppercase">
          Yapım Aşamasında
        </span>

        <div className="space-y-2">
          <h1 className="text-2xl font-black text-slate-900 tracking-tight">{title}</h1>
          <p className="text-xs md:text-sm text-slate-500 font-medium leading-relaxed max-w-md mx-auto">
            {description}
          </p>
        </div>

        <div className="flex flex-col sm:flex-row gap-2 justify-center pt-2">
          <button
            type="button"
            onClick={() => router.push('/garage')}
            className="bg-indigo-600 hover:bg-indigo-700 text-white font-bold text-xs px-6 py-3 rounded-xl transition-colors cursor-pointer"
          >
            Garajıma Dön
          </button>
          <button
            type="button"
            onClick={() => router.push('/')}
            className="bg-white hover:bg-slate-50 text-slate-800 border border-gray-200 font-bold text-xs px-6 py-3 rounded-xl transition-colors cursor-pointer"
          >
            Anasayfa
          </button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Adım 2: Altı yer tutucu sayfayı oluştur**

Her dosya aynı desendedir; yalnızca `title` ve `description` değerleri farklıdır. Şablon:

```jsx
'use client';

import React from 'react';
import ComingSoon from '@/components/common/ComingSoon';

export default function Page() {
  return <ComingSoon title="BAŞLIK" description="AÇIKLAMA" />;
}
```

Altı dosya ve kullanılacak tam değerler:

| Dosya | `title` | `description` |
|---|---|---|
| `src/app/(shell)/dashboard/page.js` | `Bana Özel Özet` | `Araçlarınızın durumunu, yaklaşan tarihleri ve bakım özetini tek ekranda toplayan panel hazırlanıyor.` |
| `src/app/(shell)/query-history/page.js` | `Sorgulama Geçmişim` | `PIN ile sorguladığınız araçların kaydı burada listelenecek.` |
| `src/app/(shell)/packages/page.js` | `Paketlerim & Ödemeler` | `Üyelik paketleriniz, ödeme geçmişiniz ve faturalarınız bu ekranda yer alacak.` |
| `src/app/(shell)/account/page.js` | `Hesabım` | `Ad, soyad, telefon ve şifre bilgilerinizi düzenleyebileceğiniz ekran hazırlanıyor.` |
| `src/app/(shell)/insurance-offer/page.js` | `Sigorta Teklifleri` | `Aracınızın poliçe tarihlerine göre size uygun sigorta ve kasko tekliflerini burada göreceksiniz.` |
| `src/app/(shell)/maintenance-planner/page.js` | `Bakım Planlayıcı` | `Kilometre ve tarih bilgilerinize göre yaklaşan bakımlarınızı planlayan araç hazırlanıyor.` |

- [ ] **Adım 3: Doğrula**

Çalıştır: `npm run dev`. Oturum açık olarak:

1. Navbar → hesap menüsü → "Bana Özel Özet" → `/dashboard` açılıyor, "Yapım Aşamasında" kartı görünüyor (eskiden sessizce pazaryerine düşerdi)
2. Aynı menüden "Sorgulama Geçmişim", "Paketlerim & Ödemeler", "Hesabım" → hepsi kendi adresini açıyor ve doğru başlığı gösteriyor
3. `http://localhost:3000/insurance-offer` ve `/maintenance-planner` → kartlar açılıyor
4. Kartlardaki "Garajıma Dön" ve "Anasayfa" butonları çalışıyor
5. Geri tuşu her birinde bir önceki sayfaya dönüyor
6. Konsolda yeni hata yok

- [ ] **Adım 4: Commit**

```bash
git add src/components/common/ComingSoon.jsx "src/app/(shell)/dashboard" "src/app/(shell)/query-history" "src/app/(shell)/packages" "src/app/(shell)/account" "src/app/(shell)/insurance-offer" "src/app/(shell)/maintenance-planner"
git commit -m "feat(routing): 6 yer tutucu route ve ComingSoon bileşeni

Menüden tıklanan ekranların sessizce pazaryerine düşmesi giderildi."
```

---

### Görev 6: Eski monolitin emekliye ayrılması

**Dosyalar:**
- Oluştur: `src/app/(shell)/page.js`
- Sil: `src/app/page.js`
- Değiştirilmez: `MarketplaceView.jsx`

**Arayüzler:**
- Kullanır: `MarketplaceView({ onSelectVehicle, onNavigateToGarage, onNavigateToVerify, onNavigateToInsurance, onNavigateToMaintenance, onOpenCreateListingModal })`
- Üretir: `/` route'u (shell grubunda, navbar layout'tan gelir)

**Bu görev geçiş dönemini kapatır.** Bundan sonra tek navigasyon yolu URL'dir; `viewState` mantığı tamamen kalkar.

- [ ] **Adım 1: Yeni anasayfayı oluştur**

`src/app/(shell)/page.js`. Eski `page.js`'in 436 satırı yerine yalnızca vitrin kalır; navbar layout'tan gelir.

```jsx
// =========================================================================
// OTO-CV ANASAYFA ROUTE'U ((shell)/page.js)
// İşlev: Pazaryeri vitrinini açar. Navbar (shell) layout'undan gelir.
// =========================================================================

'use client';

import React from 'react';
import { useRouter } from 'next/navigation';
import MarketplaceView from '@/components/marketplace/MarketplaceView';

export default function HomePage() {
  const router = useRouter();

  return (
    <MarketplaceView
      onSelectVehicle={(item) => router.push(`/details/${encodeURIComponent(item.plate_number)}`)}
      onNavigateToGarage={() => router.push('/garage')}
      onNavigateToVerify={() => router.push('/verify')}
      onNavigateToInsurance={() => router.push('/insurance-offer')}
      onNavigateToMaintenance={() => router.push('/maintenance-planner')}
      onOpenCreateListingModal={() => router.push('/add-vehicle/step1')}
    />
  );
}
```

- [ ] **Adım 2: Eski monoliti sil**

```bash
git rm "src/app/page.js"
```

- [ ] **Adım 3: Şifre kurtarma radarının artık gerekmediğini doğrula**

Eski `page.js` içinde `?type=recovery` parametresini yakalayan bir blok vardı (satır 44-52). Görev 4'te `/reset-password` gerçek bir sayfa olduğu için bu radar gereksizdir ve dosyayla birlikte silinmiştir. Ek işlem gerekmez.

- [ ] **Adım 4: Tam regresyon turu**

Çalıştır: `npm run dev`. Bu, tüm geçişin son doğrulamasıdır.

Oturum **kapalı** olarak:
1. `/` → pazaryeri vitrini navbar ile açılıyor
2. Vitrinden bir araca tıkla → `/details/<PLAKA>`, ziyaretçi görünümü
3. Geri tuşu → vitrine dönüyor
4. `/verify` → PIN formu; gerçek PIN ile sorgula → detay açılıyor
5. `/garage` yaz → `/login`'e yönleniyor

Oturum **açık** olarak:
6. Navbar → "Tescilli Taşıtlarım (Garaj)" → `/garage`
7. Kendi aracına vitrinden tıkla → **sahip görünümü** açılıyor (spec 7.3: bu bilinçli değişiklik)
8. Garaj → araç ekle → `/add-vehicle/step1`; geri → `/garage`
9. Karne → "Paylaş" → "Linki Kopyala" → yeni sekmede aç → araç detayı açılıyor
10. Hesap menüsündeki 6 bağlantı → kendi adreslerini açıyor
11. Çıkış yap → `/` açılıyor, navbar "Giriş Yap / Hesap Aç" gösteriyor

Her adımda: ileri/geri tuşları doğru çalışıyor, F5 aynı sayfada kalıyor, konsolda yeni hata yok.

- [ ] **Adım 5: Üretim derlemesini doğrula**

Çalıştır: `npm run build`

Beklenen: derleme hatasız tamamlanır ve route listesinde `/`, `/garage`, `/verify`, `/verify/[pin]`, `/details/[plate]`, `/karne/[plate]`, `/login`, `/register`, `/reset-password`, `/add-vehicle/[step]` ve 6 yer tutucu görünür.

- [ ] **Adım 6: Commit**

```bash
git add "src/app/(shell)/page.js"
git commit -m "refactor(routing): eski viewState monoliti kaldırıldı

src/app/page.js (436 satır) yerine (shell)/page.js sadece vitrini
render ediyor. Navigasyonun tek yolu artık URL."
```

---

### Görev 7 (opsiyonel): Sihirbaz adımlarının URL'e bağlanması

**Dosyalar:**
- Değiştir: `src/components/marketplace/create-listing/CreateListingWizard.jsx:23` (`currentStep` başlangıç değeri) ve adım geçiş çağrıları
- Değiştir: `src/app/(full)/add-vehicle/[step]/page.js`

**Arayüzler:**
- Kullanır: `CreateListingWizard`'ın mevcut `currentStep` / `setCurrentStep` state'i
- Üretir: `/add-vehicle/step2` gibi adreslerin doğrudan çalışması

**Bu görev opsiyoneldir ve ayrı onay gerektirir.** 1111 satırlık bir bileşene dokunulan tek yer burasıdır. Yapılmazsa sihirbaz bugün olduğu gibi çalışmaya devam eder; yalnızca adımlar arası tarayıcı geri tuşu kazanılmamış olur.

**Yaklaşım:** Sihirbaza `initialStep` ve `onStepChange` prop'ları eklenir. Bileşen kendi `currentStep` state'ini korur (taslaktan devam etme mantığı `draftData.savedStep` üzerinden çalışmaya devam etmeli), her adım değişiminde `onStepChange` çağrılır ve sayfa URL'i `router.replace` ile güncellenir.

`router.push` yerine `replace` tercih edilir mi sorusu bu görev başlarken kullanıcıyla netleştirilmelidir: `push` her adımı geçmişe yazar (geri tuşu adım adım geri gider — istenen davranış), `replace` yazmaz. Varsayılan: **`push`**.

Detaylı adımlar, bu göreve başlanmasına karar verildiğinde `CreateListingWizard.jsx`'in adım geçiş noktaları (satır 640, 698, 710, 728, 739, 754, 766) okunarak yazılacaktır. Plan bu görevi bilinçli olarak açık bırakır çünkü diğer altı görev tamamlandığında sihirbazın URL ihtiyacı yeniden değerlendirilmelidir.

---

## Öz-denetim

**Spec kapsamı:** Spec'in URL haritasındaki 16 ekranın tamamı bir göreve bağlandı — vitrin (G6), garaj + ilanlar (G3), verify + verify/[pin] (G2), details + karne (G1), login + register + reset-password (G4), add-vehicle (G3, URL eşlemesi G7), 6 yer tutucu (G5). Spec 7.1 (plaka kodlama) G1'de `encodeURIComponent` ile, 7.2 (araç verisi çekme) G1'de, 7.3 (`isPublicMode` sahiplikten türetme) G1 Adım 3'te, 7.4 (`/verify/[pin]` yönlendirmesi) G2 Adım 3'te, 7.5 (oturum kontrolü) G3'te, 7.6 (`reset-password` çakışması) G4'te karşılandı.

**Yer tutucu taraması:** Görev 7 dışında hiçbir adımda "TBD" veya belirsiz talimat yok; her kod adımı çalıştırılabilir kod içeriyor. Görev 7 bilinçli olarak açık bırakılmıştır ve bu durum başlığında ve gövdesinde açıkça belirtilmiştir — o göreve başlamak ayrı onay gerektirir.

**Tip ve isim tutarlılığı:** `onVehicleFound(car, 'public')` ikinci argümanı bileşende korunuyor ama route sayfaları kullanmıyor (rol artık sahiplikten geliyor) — bu bilinçli, bileşenin imzası değişmedi. `initialPin` prop'u G2 Adım 1'de tanımlanıp Adım 3'te kullanılıyor. `ComingSoon({ title, description })` G5 Adım 1'de tanımlanıp Adım 2'de kullanılıyor. `plate` param adı `[plate]` klasör adıyla, `pin` param adı `[pin]` ile, `step` param adı `[step]` ile eşleşiyor.

**Bilinen geçiş dönemi kusurları (kabul edilmiş):**
- Görev 3 sonunda oturumsuz `/garage` denemesi `/login`'e yönlenir ama o sayfa Görev 4'te gelir → arada 404. Görev 3 Adım 4 madde 9'da açıkça beklenen davranış olarak yazıldı.
- Görev 1–5 boyunca anasayfada eski navbar ve yeni route'lar birlikte yaşar. Görev 6 tekilleştirir.
