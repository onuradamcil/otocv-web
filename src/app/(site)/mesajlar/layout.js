export const metadata = {
  title: 'Mesajlarım',
  description: 'Araç sahipleriyle yaptığınız yazışmalar.',
  robots: { index: false, follow: false },
};

export default function MesajlarLayout({ children }) {
  return children;
}
